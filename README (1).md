# Backend HostPilot

**Seul fichier serveur à exécuter :** `api.js`

```bash
node src/api.js
# ou depuis la racine : npm start
```

Tout autre backend (Express/SQLite) a été déplacé vers un ancien backend non livré.
